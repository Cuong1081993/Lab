

const country = "America";
const population = 50;
const isIsland = true;
let language = 'English';

if (population == 50 && country === 'America' && isIsland && language =='Vietnamese') {
    console.log(`You should live at ${country}`);
} else {
    console.log(`${country} does not meet your criteria`);
}